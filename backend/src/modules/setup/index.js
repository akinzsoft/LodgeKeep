'use strict';

/**
 * `src/modules/setup`'s public surface — PLAN.md Phase 1. Other code
 * imports from here, never from the files behind it.
 */

const { setupRouter } = require('./routes');

module.exports = { setupRouter };
