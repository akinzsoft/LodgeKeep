'use strict';

/**
 * `src/modules/profiles`' public surface — PLAN.md Phase 2 gap closure.
 * Other code imports from here, never from the files behind it.
 */

const { profilesRouter } = require('./routes');

module.exports = { profilesRouter };
