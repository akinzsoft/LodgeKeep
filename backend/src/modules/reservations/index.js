'use strict';

/**
 * `src/modules/reservations`'s public surface — PLAN.md Phase 2. Other
 * code imports from here, never from the files behind it.
 */

const { reservationsRouter } = require('./routes');

module.exports = { reservationsRouter };
