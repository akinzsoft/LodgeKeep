node_modules/
.env
.env.local
.env.*.local
# Production Docker deployment secrets (docker-compose.prod.yml's own
# --env-file) — .env.*.local above does NOT match this literal filename.
.env.production
coverage/
dist/
*.log
.DS_Store

# PLAN.md Phase 5 tenant offboarding — generated data-export bundles,
# never committed (real tenant data, local-disk storage per EXPORT_STORAGE_DIR).
backend/storage/exports/

# PLAN.md Phase 5 data migration — uploaded customer CSV files, never
# committed (real customer data, local-disk storage per IMPORT_STORAGE_DIR).
backend/storage/imports/
# POS menu item photos — uploaded per tenant, local-disk storage per MENU_IMAGE_STORAGE_DIR.
backend/storage/menu-images/
# Property logos — uploaded per property, local-disk storage per PROPERTY_LOGO_STORAGE_DIR.
backend/storage/property-logos/
